# 스티커 담벼락 질문 성장 기능 구현 계획

## 구현 현황 — 2026-09-11

- 완료: 루트 웹앱을 기준 소스로 선언하고 배포 Worker의 위치를 문서화
- 완료: 담벼락 주제·핵심 낱말·질문 코치 설정
- 완료: AI 없는 질문 시작 도움과 초안·패널 상태 보존
- 완료: 학생용 질문 다듬기 UI, 구조화 응답, 인증·참여 확인, rate limiter 필수 처리
- 완료: 공개 질문과 비공개 학습 기록·최소 사용성 이벤트 분리
- 완료: 교사가 여는 질문 이어가기와 학생의 질문 선택 이유 기록
- 완료: Worker 입력·응답 단위 테스트와 브라우저 테스트 장면
- 완료: Java 21 설치 후 Firebase 보안 규칙 에뮬레이터 검증 통과
- 완료: Firebase 규칙 게시, Cloudflare `AI_BURST_LIMITER`(학생별 10초 1회)·`AI_ROOM_LIMITER`(담벼락별 60초 5회) 연결, 최신 Worker 배포
- 완료: 공개 Worker CORS·비인증 차단 확인, GitHub Pages 최신 화면 및 교사용 질문 코치 설정 확인, 학생용 질문 시작 도움 테스트 장면 확인
- 완료: 교사가 바로 읽는 질문 코치 안내 문구와 학생 주도형 AI 예시 원칙 정리
- 완료: Wrangler 설정 파일을 기준으로 rate limiter 바인딩을 관리하고, `--keep-vars` 배포로 기존 비밀 값·변수를 보존
- 완료: AI 예시가 빈칸형 질문 뼈대를 지키지 않을 때 안전한 한 칸 질문 뼈대로 보정
- 완료: 별도 익명 학생 세션으로 참여·색 선택·포스트잇·AI 질문 다듬기·질문 저장·학습 기록 저장 종단 검증
- 준비 완료: [질문 코치 시범수업 검증 체크리스트](질문-코치-시범수업-검증-체크리스트.md)
- 다음 단계: 소규모 시범수업에서 학생의 실제 사용성·질문 수정 양상을 관찰하고, 수업 목표를 반영한 교사 분석과 차시 간 학생 연결을 설계

## 1. 문서 목적

이 문서는 [질문-성장-개편안](질문-성장-개편안.md)의 `현재 적용안`을 실제 프로젝트에 반영하기 위한 개발 계획이다.

학생이 친구의 글에서 궁금한 부분을 찾고, 질문을 또렷하게 다듬고, 답을 들은 뒤 다음 질문으로 이어 가는 흐름을 구현한다. 질문왕, 점수, 배지, 학생 수준·레벨 구분은 이번 계획에서 제외한다.

## 2. 현재 구현 기준

현재 앱은 다음 구조를 사용한다.

- `app.js`: 교사·학생 화면, 활동 단계, 질문 작성과 Firebase 저장, 교사용 AI 호출
- `styles.css`: 반응형 화면과 포스트잇 UI
- `database.rules.json`: 담벼락 참여자·질문·교사 기록의 읽기와 쓰기 권한
- `tests/database.rules.test.mjs`: Firebase Realtime Database 보안 규칙 테스트
- `sticker-wall-publish/cloudflare-worker/worker.js`: Solar API를 호출하는 배포용 Worker
- `ai-helper.js`: 교사 컴퓨터에서 실행하는 선택형 로컬 AI 도우미

현재 학생 질문은 질문 단계에서 80자 이내 한 줄을 입력하고 바로 저장된다. 배포 Worker의 `/assist`는 교사 Firebase UID만 허용하므로 학생은 AI 기능을 호출할 수 없다. 질문 성장 기능을 위해서는 학생 입력 UI, 임시 상태, Worker 권한, Firebase 규칙을 함께 수정해야 한다.

루트와 `sticker-wall-publish`에 앱 파일이 중복되어 있다. README와 현재 실행 명령은 루트를 가리키므로, 0단계에서 실제 배포 원본을 확인한 뒤 한쪽을 기준 소스로 선언한다. 확인 전에는 두 사본을 동시에 수동 수정하지 않는다.

## 3. 이번 구현 범위

### 1차 출시 범위

- 교사가 담벼락별로 질문 코치 사용 여부 설정
- 친구의 글에서 궁금한 부분을 입력하는 질문 시작 도움
- AI 없이 즉시 쓸 수 있는 뜻·이유·근거 질문 틀
- 작성한 질문을 `더 또렷하게` 또는 `다른 방향으로` 다듬기
- AI 힌트와 선택형 예시 보기
- 원문 유지·수정·예시 사용 후 학생의 최종 등록
- AI 오류와 재렌더링이 발생해도 질문 초안 보존
- 텍스트를 담지 않는 최소 사용성 이벤트
- 학생 요청의 인증, 담벼락 참여 확인, 기능 허용 확인, 횟수 제한

### 후속 출시 범위

- 교사가 여는 질문 이어가기 시간
- 원래 질문과 후속 질문 연결
- 학생이 다시 보고 싶은 질문 하나와 선택 이유 기록
- 수업 목표를 반영한 교사용 질문 흐름과 성찰 초안
- 검증된 학생 연결 방식이 마련된 뒤 차시 간 성장 기록

### 보류 범위

- 질문왕과 자동 수상
- 점수, 등수, 배지
- 쉬움·보통·도전 등 학생 레벨 구분
- 질문 길이와 AI 사용량을 이용한 역량 평가
- AI가 만든 질문의 자동 등록
- 출석번호·이름을 AI에 전송하는 기능

## 4. 1차 출시 학생 경험

### 4.1 기본 질문 작성

현재 포스트잇별 질문 입력란을 다음 구조로 바꾼다.

```text
[궁금한 점을 적어요                              ]
[질문 시작 도움]  [질문 다듬기]  [질문 등록]
```

- `질문 시작 도움`은 빈 입력에서도 사용할 수 있다.
- `질문 다듬기`는 초안이 있을 때 활성화한다.
- `질문 등록`은 지금처럼 학생이 최종 문장을 확인한 뒤 실행한다.
- 질문 코치가 꺼져 있거나 AI 요청이 실패해도 직접 질문 등록은 항상 가능하다.

### 4.2 질문 시작 도움

학생에게 먼저 다음 문장을 보여 준다.

`친구의 글에서 더 알고 싶은 말이나 부분을 적어 보세요.`

학생이 짧은 낱말이나 구절을 적으면 다음 세 방향을 보여 준다.

- 무슨 뜻인지 궁금해요.
- 왜 그런지 궁금해요.
- 어떻게 알았는지 궁금해요.

각 방향은 클라이언트에 저장된 질문 틀로 동작한다. 첫 출시에서는 Solar API를 호출하지 않는다.

```text
뜻: 여기서 ‘{궁금한 부분}’은 무슨 뜻인가요?
이유: ‘{궁금한 부분}’은 왜 그런가요?
근거: ‘{궁금한 부분}’은 어떻게 알게 되었나요?
```

선택 결과는 질문 입력란에 복사한 뒤 학생이 고칠 수 있게 한다. 포스트잇 본문을 자동으로 분석하거나 특정 낱말을 AI가 임의로 선택하지 않는다.

### 4.3 질문 다듬기

학생이 초안을 쓴 뒤 두 방향 중 하나를 선택한다.

| 선택 | AI가 하는 일 |
| --- | --- |
| 더 또렷하게 | 원래 궁금함을 유지하면서 빠진 대상·상황·조건을 스스로 생각할 힌트 제공 |
| 다른 방향으로 | 이유·변화·비교·가정·관점·근거 중 현재 질문과 다른 생각 방향 하나 제안 |

AI 응답 카드에는 다음 정보만 표시한다.

```text
지금 궁금한 점: {현재 질문의 중심을 짧게 설명}
생각 힌트: {학생이 스스로 보충할 내용 한 가지}
[내가 다시 써 보기]
[예시 보기]
```

`예시 보기`를 누르면 같은 응답에 포함되어 있던 예시 하나를 펼친다. AI 호출을 두 번 하지 않으면서 예시 노출은 학생이 선택하게 한다.

학생은 다음 행동 중 하나를 선택할 수 있다.

- 원래 질문으로 돌아가기
- 질문 입력란에서 직접 수정하기
- 예시를 입력란으로 가져온 뒤 확인·수정하기
- 최종 질문 등록하기

AI가 원문이 이미 충분히 또렷하다고 판단하면 수정을 강요하지 않고 `이 질문은 지금도 뜻이 잘 전달돼요`라고 알린다.

### 4.4 초안과 화면 상태

현재 `sessionStorage`의 질문 초안 저장 구조를 확장한다. 포스트잇별로 다음 임시 상태를 보존한다.

```json
{
  "text": "학생이 작성 중인 질문",
  "panel": "closed | starter | refine",
  "focusText": "궁금한 부분",
  "refineGoal": "clarify | explore",
  "aiResult": null
}
```

Firebase 실시간 갱신으로 화면이 다시 그려져도 입력값, 열려 있던 도움 패널, 커서 위치를 복원한다. AI 결과에는 수업 텍스트가 포함되므로 브라우저 영구 저장소에는 남기지 않고 현재 탭의 세션에서만 유지한다. 질문 등록이 끝나면 해당 초안과 AI 결과를 지운다.

### 4.5 접근성과 교실 사용성

- 버튼과 선택지는 키보드와 터치로 사용할 수 있게 한다.
- AI 요청 중인 버튼에 진행 상태를 텍스트로 표시한다.
- 결과가 도착하면 `aria-live`로 알리되 입력 포커스를 강제로 이동하지 않는다.
- 작은 화면에서는 도움 패널을 해당 질문 입력란 바로 아래에 표시한다.
- 한 화면에 여러 질문 폼이 있으므로 현재 작업 중인 포스트잇 하나만 도움 패널을 연다.
- 학생이 빠르게 읽을 수 있도록 설명 문장은 두 문장, 제안은 한 개를 기본으로 한다.

## 5. 담벼락 설정 변경

새 담벼락 만들기에 다음 필드를 추가한다.

- 담벼락 제목: 기존 필드 유지
- 발표 주제: 선택 입력, 최대 80자
- 핵심 낱말: 선택 입력, 쉼표로 구분, 최대 5개
- 질문 코치 사용: 기본 켜짐

저장 예시는 다음과 같다.

```json
{
  "title": "우리 반의 오늘 생각",
  "topic": "식물이 자라는 데 필요한 조건",
  "keyTerms": ["햇빛", "물", "온도"],
  "questionCoachEnabled": true
}
```

주제와 핵심 낱말은 AI에 맥락을 제공할 뿐 정답 자료로 사용하지 않는다. 기존 담벼락에서 값이 없으면 제목과 포스트잇 본문만 사용한다. 교사 화면에는 수업 중 질문 코치를 켜고 끄는 제어를 둔다. 끈 뒤에도 이미 작성 중인 학생 초안은 사라지지 않으며 직접 등록할 수 있다.

## 6. 학생용 AI API

### 6.1 요청 모드

기존 `/assist` 경로에 학생용 `question-refine` 모드를 추가하되, 교사용 모드와 권한 검사를 분리한다. 질문 시작 도움은 로컬 틀을 사용하므로 `starter` AI 모드는 만들지 않는다.

요청 예시:

```json
{
  "mode": "question-refine",
  "roomId": "STICKER-ABC123",
  "postId": "post-id",
  "goal": "clarify",
  "question": "왜 식물이 안 자라요?",
  "context": {
    "title": "우리 반 과학 발표",
    "topic": "식물이 자라는 데 필요한 조건",
    "keyTerms": ["햇빛", "물"],
    "postText": "창가와 서랍 속에서 강낭콩을 길렀습니다."
  }
}
```

Worker는 클라이언트가 보낸 맥락을 그대로 신뢰하지 않는다. 토큰과 `roomId`로 참여 상태와 질문 코치 허용 상태를 확인하고, 입력 길이와 허용 필드를 정리한 뒤 Solar에 전달한다.

### 6.2 응답 구조

Solar 응답은 일반 텍스트가 아니라 JSON으로 제한하고 Worker가 형태와 길이를 검증한다.

```json
{
  "observation": "식물이 자라지 않은 이유를 궁금해하고 있어요.",
  "hint": "어떤 식물인지와 어디에서 키웠는지 떠올려 보세요.",
  "example": "창가와 서랍 속 강낭콩의 자람이 달랐던 이유는 무엇일까요?",
  "alreadyClear": false
}
```

검증 조건은 다음과 같다.

- 네 필드 외의 출력은 버린다.
- `observation`, `hint`, `example`은 각각 100자 이내의 한국어 일반 텍스트로 제한한다.
- 답, 사실 해설, 학생 평가, 점수 표현을 허용하지 않는다.
- `example`은 포스트잇과 학생 질문에 없는 고유 사실을 만들어 넣지 않는다.
- JSON 파싱이나 검증에 실패하면 한 번만 형식 교정을 시도하고, 다시 실패하면 학생에게 재시도 안내를 돌려준다.

### 6.3 프롬프트 원칙

- 학생의 질문 의도를 바꾸지 않는다.
- `clarify`에서는 대상·상황·조건을 찾게 돕는다.
- `explore`에서는 다른 사고 방향 하나만 제안한다.
- 사실 질문과 짧은 질문도 필요한 질문으로 인정한다.
- 원문이 명확하면 수정 필요가 없다고 말할 수 있다.
- 학생의 질문을 칭찬하거나 등급화하지 않는다.
- 초등학교 5학년이 바로 이해할 말로 쓴다.
- 질문의 답을 제공하지 않는다.

## 7. 인증, 참여 확인, 사용량 제한

학생 AI 호출은 현재의 익명 Firebase ID 토큰을 사용한다.

Worker 처리 순서는 다음과 같다.

1. Firebase ID 토큰의 서명, 발급자, 프로젝트, 만료를 검증한다.
2. 토큰의 UID가 요청한 담벼락의 `roomMembers/{roomId}/{uid}`에 등록되어 있는지 확인한다.
3. 해당 담벼락의 `questionCoachEnabled`가 `true`이고 현재 단계가 `voting`인지 확인한다.
4. 요청의 `postId`가 해당 담벼락에 존재하는지 확인한다.
5. UID와 담벼락을 기준으로 사용량 제한을 적용한다.
6. 필요한 최소 텍스트만 Solar API에 보낸다.

참여 확인을 위해 Firebase 규칙에서 학생이 자신의 `roomMembers/{roomId}/{uid}`만 읽을 수 있게 허용하거나, Worker가 Firebase REST API를 통해 동일한 권한 검사를 수행할 수 있는 서버 측 방식을 마련한다. 전체 참여자 목록은 학생에게 공개하지 않는다.

초기 제한값은 다음으로 시작하고 실제 수업 데이터를 보고 조정한다.

- 학생 1명당 담벼락별 최대 5회
- 동일 UID의 연속 요청 사이 최소 3초
- 질문 최대 80자, 포스트잇 최대 80자, 주제 최대 80자
- 요청 본문 최대 4KB
- 응답 대기 최대 15초

사용량 제한은 Worker 인스턴스 메모리에만 두지 않는다. Cloudflare의 지속 가능한 rate limiting 수단을 사용하고, 제한에 도달하면 직접 작성과 기본 질문 틀을 계속 사용할 수 있게 안내한다.

## 8. Firebase 데이터 설계

### 8.1 공개 질문

친구들이 읽는 질문에는 현재 필드를 유지한다.

```text
rooms/{roomId}/posts/{postId}/questions/{questionId}
  text
  authorId
  createdAt
```

AI 사용 여부와 성장 기록을 공개 질문 아래에 넣지 않는다.

### 8.2 비공개 학습 기록

교사와 작성 학생만 접근하는 별도 경로를 둔다.

```text
studentLearning/{roomId}/{uid}/questions/{questionId}
  postId
  helpUsed        none | starter | ai-hint | ai-example
  refineGoal      none | clarify | explore
  createdAt

studentLearning/{roomId}/{uid}/reflections/{questionId}
  reason
  createdAt
```

AI 제안 전문, 학생의 버린 초안, 원문과 최종문의 전체 비교본은 저장하지 않는다. `helpUsed`는 학생의 능력을 판정하는 값이 아니라 기능 사용을 이해하기 위한 정보다.

질문 저장과 학습 기록 저장은 가능한 한 하나의 다중 경로 `update`로 처리한다. 일부 저장이 실패해 공개 질문과 기록이 어긋나더라도 질문 등록 자체를 실패로 돌리지 않고, 교사 분석에서는 기록이 없는 질문을 정상 질문으로 취급한다.

### 8.3 최소 사용성 이벤트

미등록 흐름을 확인해야 할 때만 텍스트 없는 이벤트를 저장한다.

```text
usageEvents/{roomId}/{uid}/{postId}/{eventType}
  createdAt
```

- 학생은 자신의 경로에 허용된 이벤트 종류를 포스트잇마다 한 번만 기록할 수 있다.
- 학생은 다른 학생의 이벤트를 읽을 수 없다.
- 교사만 담벼락 전체 이벤트를 읽을 수 있다.
- 이벤트 보관 기간과 삭제 절차를 README에 명시한다.
- 실제 제품 판단에 필요하지 않으면 이 경로 전체를 구현하지 않는다.

## 9. 질문 이어가기와 성찰

1차 출시 안정화 뒤 기존 3단계 흐름을 유지하면서 `followupOpen` 설정을 추가한다. 교사가 발표 후 `질문 이어가기 열기`를 누르면 학생은 자신이 작성한 질문에 후속 질문 하나를 남길 수 있다.

```text
rooms/{roomId}/posts/{postId}/questions/{questionId}/followups/{uid}
  text
  authorId
  createdAt
```

후속 질문 작성자는 원래 질문 작성자와 같아야 한다. 한 학생은 자신의 질문마다 후속 질문 하나를 남길 수 있다. 교사가 기능을 열었을 때만 생성할 수 있고, 80자 제한과 기존 안전 원칙을 적용한다.

학생은 활동 뒤 자신이 작성한 질문 중 하나를 `다시 보고 싶은 질문`으로 선택하고 이유를 한 문장으로 남길 수 있다. 이 내용은 해당 학생과 교사만 읽는다. 학급 공개나 자동 평가에는 사용하지 않는다.

## 10. 교사용 분석 계획

교사용 분석은 학생 기능과 질문 이어가기의 수업 검증 뒤 구현한다.

교사가 담벼락 생성 시 선택적으로 다음을 입력한다.

- 수업에서 학생들이 살펴보기를 기대한 내용
- 오늘 질문 활동의 목적

분석은 질문 유형의 균형을 목표로 하지 않는다. 수업 목표와 실제 질문을 함께 보고 다음을 제공한다.

- 학생들이 실제로 궁금해한 내용
- 뜻·근거·이유·변화·비교·가정·관점 등 나타난 질문 방향
- 답을 들은 뒤 이어진 질문의 주제
- 교사가 다음 차시에 선택할 수 있는 활동 한 가지

목표가 입력되지 않았으면 유형별 현황만 설명하고 부족함이나 성장으로 판정하지 않는다. 결과에는 대표 질문 ID를 포함해 교사가 원문을 확인할 수 있게 한다. 학생 개인별 점수나 순위는 생성하지 않는다.

### 10.1 분석 입력과 공개 범위

새 담벼락에서 교사는 선택적으로 `수업에서 함께 살펴볼 점`과 `오늘 질문 활동의 목적`을 입력한다. 둘 다 비워도 활동은 그대로 시작할 수 있다. 이 값은 교사 화면과 분석 요청에만 사용하며, 학생에게 목표 달성 여부를 표시하지 않는다.

분석 요청에는 담벼락 제목·주제·핵심 낱말·선택한 수업 목표와 함께 다음만 보낸다.

- 포스트잇 ID와 본문
- 질문 ID·원 질문 ID·최종 질문 본문
- 답글과 후속 질문 본문
- 도움 사용 여부의 집계값(학생별 기록·UID는 제외)

출석번호, 색 이름, Firebase UID, 학생별 질문 수, 학생 성찰의 선택 이유는 분석 요청에 넣지 않는다. 분석 결과는 기본적으로 화면에만 두고, 교사가 `성찰 초안 복사`를 누를 때에만 클립보드로 보낸다. 자동 저장·자동 공유·학생 화면 공개는 하지 않는다.

### 10.2 교사용 분석 응답 계약

Solar 응답은 일반 문단이 아니라 다음 구조의 JSON으로 받고, Worker가 길이·필수 필드·질문 ID를 검증한다.

```json
{
  "goalUsed": true,
  "classFocus": "학생들이 햇빛과 식물의 자람 관계를 궁금해했습니다.",
  "questionDirections": [
    {"label": "이유를 묻기", "summary": "자라지 않은 까닭을 묻는 질문이 이어졌습니다.", "questionIds": ["q1", "q2"]}
  ],
  "connectedQuestionIds": ["q3"],
  "nextActivity": "두 조건을 나란히 보고, 달라진 점을 묻는 질문을 한 가지 더 써 봅니다.",
  "reflectionDraft": "학생들은 식물이 자라는 조건의 이유를 주로 궁금해했습니다. 다음 차시에는 두 관찰 조건을 비교하며 질문을 이어 볼 수 있습니다.",
  "caution": ""
}
```

`questionDirections`는 최대 세 방향이며, 라벨은 뜻·근거·이유·변화·비교·가정·관점·기타 중 하나로 제한한다. `questionIds`는 실제 입력에 있던 ID만 허용한다. `connectedQuestionIds`는 답글이나 후속 질문으로 이어진 원 질문만 가리킨다. 입력된 수업 목표가 없으면 `goalUsed`는 `false`여야 하며, `nextActivity`와 `reflectionDraft`에 부족함·미달·성장 점수 같은 판정을 넣지 않는다.

### 10.3 교사 화면 흐름

1. 발표가 끝난 뒤 교사가 `수업 되돌아보기`를 연다.
2. 화면에는 수업 목표(있는 경우), 학급의 관심, 질문 방향, 이어진 질문, 다음 활동 하나가 차례로 보인다.
3. 각 방향에 붙은 `원문 보기`를 누르면 해당 질문이 있는 포스트잇으로 이동한다.
4. 교사는 결과를 고치거나 필요한 부분만 선택해 `성찰 초안 복사`를 누른다.

분석은 버튼을 눌렀을 때만 실행하고, 수업 중 자동 팝업으로 띄우지 않는다. 결과에는 ‘가장 좋은 질문’, ‘부족한 학생’, 순위, 개인별 목록을 만들지 않는다. AI가 근거가 불충분한 해석을 하면 `caution`에 교사 확인이 필요함을 알리고, 그 문장을 성찰 초안에 자동 포함하지 않는다.

### 10.4 구현 전 검증 기준

- 목표가 있는 경우에도 목표 달성 여부를 학생 개인에게 귀속하지 않는다.
- 목표가 없는 경우에는 질문 방향의 개수만 묘사하고, 적은 방향을 결핍으로 해석하지 않는다.
- 대표 질문 링크가 실제 원문 질문으로만 이동하는지 확인한다.
- 0개 질문·1개 질문·후속 질문만 있는 경우에도 안전한 빈 상태 안내를 제공한다.
- 분석 요청과 응답을 Firebase에 영구 저장하지 않고, 교사가 복사한 결과만 별도 수업 기록에 남길 수 있게 한다.

## 11. 보안 규칙 변경 계획

`database.rules.json`과 `database.rules.draft.json`을 함께 검토하고, 배포 규칙은 테스트 통과 후 갱신한다.

필수 규칙 테스트는 다음과 같다.

- 담벼락 구성원은 질문 단계에서 자신의 질문만 새로 만들 수 있다.
- AI 도움 정보 없이도 기존 질문 형식이 계속 저장된다.
- 학생은 자신의 비공개 학습 기록만 쓰고 읽을 수 있다.
- 학생은 다른 학생의 학습 기록과 사용성 이벤트를 읽거나 쓰지 못한다.
- 교사는 자신이 관리하는 담벼락의 기록만 읽을 수 있다.
- 허용하지 않은 필드, 이벤트 종류, 지나치게 긴 문장은 거부된다.
- 질문 이어가기가 닫혔거나 작성자가 다르면 후속 질문을 만들 수 없다.
- 기존 담벼락과 기존 학생 세션이 계속 읽히고 질문 활동에 참여할 수 있다.

현재 규칙에는 하나의 고정 `TEACHER_UID`가 사용된다. 이번 기능만 추가하면서 다중 교사 구조까지 넓히지는 않되, 새 비공개 경로에서도 동일한 교사 권한 모델을 일관되게 적용한다. 다중 교사 지원은 별도 프로젝트로 분리한다.

## 12. 코드 변경 단위

### `app.js`

- 담벼락 생성 폼에 주제·핵심 낱말·질문 코치 설정 추가
- 포스트잇별 질문 코치 상태 관리 함수 분리
- 질문 시작 도움 패널과 로컬 틀 적용
- 질문 다듬기 요청, 로딩, 오류, 응답 표시
- 예시 펼치기와 입력란 반영
- 초안·열린 패널·포커스 복원 확장
- 질문과 비공개 학습 기록의 저장 처리
- 후속 단계에서 질문 이어가기와 성찰 UI 추가
- 내려받기 스키마 버전을 올리고 새 기록의 공개 범위를 구분

### `styles.css`

- 질문 코치 버튼과 도움 카드
- 선택 상태, 로딩, 오류, 접힌 예시
- 모바일·아이패드 터치 영역과 한 포스트잇 내 세로 배치
- 키보드 포커스와 고대비 상태

### `sticker-wall-publish/cloudflare-worker/worker.js`

- 교사 토큰 검사와 일반 Firebase 토큰 검사를 분리
- 학생용 `question-refine` 모드 추가
- 담벼락 참여·기능 허용·활동 단계·포스트잇 존재 확인
- 학생 요청 전용 입력 정리와 크기 제한
- Solar JSON 응답 검증
- 사용자·담벼락별 지속형 요청 제한
- 학생 오류 메시지에서 내부 API 정보 제거

### `database.rules.json`과 테스트

- 학생 자신의 참여 정보 확인 권한 검토
- 비공개 학습 기록과 선택적 사용성 이벤트 경로 추가
- 후속 질문과 질문 이어가기 설정 규칙 추가
- 기존 규칙 회귀 테스트와 새 권한 테스트 추가

### 문서

- README의 학생 활동 흐름과 AI 데이터 처리 설명 수정
- Worker 배포 환경 변수와 사용량 제한 설정 기록
- 교사용 수업 전 점검에 질문 코치 켜기·직접 등록·오류 복구 확인 추가
- 기록 보관·삭제 범위와 JSON 내보내기 스키마 설명 추가

## 13. 단계별 작업과 완료 조건

### 0단계 — 기준 소스와 회귀 기준 확정

작업:

- 루트와 `sticker-wall-publish`의 차이를 확인하고 기준 소스 선언
- 현재 앱의 학생 입장, 질문 등록, 답글, 교사 단계 전환을 기준 시나리오로 기록
- Firebase 규칙 테스트가 현 상태에서 통과하는지 확인

완료 조건:

- 수정할 파일과 배포할 파일의 관계가 README에 명확하다.
- 기존 질문 활동의 통과 기준이 정해져 있다.

### 1단계 — AI 없는 질문 시작 도움

작업:

- 궁금한 부분 입력과 뜻·이유·근거 틀 구현
- 포스트잇별 초안과 패널 상태 보존
- 교사의 질문 코치 켜기·끄기 구현

완료 조건:

- 네트워크와 Solar API 없이 질문 시작 도움을 사용할 수 있다.
- 실시간 갱신 뒤에도 입력 중인 질문과 도움 상태가 유지된다.
- 기존 직접 질문 등록 흐름이 유지된다.

### 2단계 — AI 질문 다듬기

작업:

- 학생 토큰·참여 확인과 요청 제한 구현
- `더 또렷하게`, `다른 방향으로` UI와 Worker 모드 구현
- 구조화 응답 검증과 오류 복구 구현
- 도움 사용 기록을 공개 질문과 분리

완료 조건:

- 비구성원, 다른 담벼락, 꺼진 기능, 질문 단계 밖의 요청이 거부된다.
- AI가 실패하거나 제한에 도달해도 작성 중인 질문을 직접 등록할 수 있다.
- 예시는 학생이 눌렀을 때만 보인다.
- 질문 답변·점수·학생 평가가 결과에 포함되지 않는다.

### 3단계 — 질문 이어가기와 학생 성찰

작업:

- 교사 제어로 후속 질문 시간 열기
- 원 질문과 후속 질문 연결
- 다시 보고 싶은 질문과 이유 기록

완료 조건:

- 학생은 자신의 질문에만 후속 질문을 남길 수 있다.
- 기능이 닫히면 새 후속 질문이 저장되지 않는다.
- 선택 이유는 작성 학생과 교사만 읽을 수 있다.

### 4단계 — 교사 분석과 성장 기록 검증

작업:

- 수업 목표 입력과 구조화된 질문 흐름 분석
- 대표 질문 확인과 성찰 초안 복사
- 차시 간 학생 연결 방식 설계·검증

완료 조건:

- 분석 결과가 원문 질문 ID와 연결된다.
- 목표가 없는 수업을 부족한 유형 중심으로 평가하지 않는다.
- 익명 UID가 달라진 학생 기록을 자동으로 합치지 않는다.

## 14. 검증 계획

### 자동 검증

- Firebase Emulator로 허용·거부 권한 테스트
- Worker 입력 정리, 토큰 검증, JSON 응답 검증 단위 테스트
- 질문 코치 상태와 질문 초안 직렬화 함수 테스트
- 기존 질문·답글·교사 제어 규칙의 회귀 테스트

### 브라우저 검증

- 데스크톱, 아이패드 폭, 휴대전화 폭
- Firebase 실시간 갱신 중 한글 조합 입력과 커서 유지
- 같은 학생이 여러 포스트잇 사이를 이동할 때 초안 분리
- 느린 응답, 시간 초과, 잘못된 JSON, 사용량 초과 상황
- 교사가 수업 중 질문 코치를 끄는 상황
- 기존 방처럼 새 설정값이 없는 데이터의 호환성

### 수업 검증

첫 시범 수업은 기능 효과보다 흐름의 부담을 확인한다.

- 학생이 도움 없이 바로 질문을 등록할 수 있는가
- ‘궁금한 부분’과 세 가지 시작 방향을 이해하는가
- AI 힌트가 학생의 원래 궁금함을 유지하는가
- 예시가 학생의 질문을 획일화하지 않는가
- 교사가 질문 시간을 계획한 시간 안에 운영할 수 있는가

다음 시범 수업부터 학생이 자신의 질문 의도를 설명하는지, 도움 선택이 달라지는지, 답을 들은 뒤 질문이 이어지는지를 관찰한다.

## 15. 출시와 되돌리기

새 기능은 담벼락별 `questionCoachEnabled` 설정 뒤에 둔다. 문제가 생기면 교사가 이 설정을 끄고 기존 직접 질문 작성 흐름으로 즉시 돌아갈 수 있어야 한다.

출시 순서는 다음과 같다.

1. 개발용 Firebase 프로젝트와 Worker에서 규칙·API 검증
2. 테스트 담벼락에서 교사 1명과 소수 기기로 사용성 확인
3. 한 학급에서 질문 시작 도움만 시범 운영
4. 같은 학급에서 AI 질문 다듬기 제한 운영
5. 결과를 검토한 뒤 질문 이어가기와 교사 분석 진행

Firebase 규칙은 기존 질문 형식을 계속 허용하도록 먼저 배포하고, 이후 앱과 Worker를 배포한다. 새 앱을 되돌려도 기존 담벼락과 질문을 읽을 수 있게 데이터 변경은 추가형으로 설계한다.

## 16. 구현 착수 순서

첫 개발 작업은 다음 묶음으로 시작한다.

1. 기준 소스 확정과 기존 규칙 테스트 실행
2. 담벼락 설정에 `topic`, `keyTerms`, `questionCoachEnabled` 추가
3. 질문 폼 상태를 별도 함수로 분리하고 초안 보존 구조 확장
4. AI 없는 질문 시작 도움 구현
5. 학생용 Worker 인증·참여 확인의 테스트부터 작성
6. 질문 다듬기 API와 UI 연결

이 묶음이 완료되면 Solar API가 없어도 학생용 질문 시작 도움을 사용할 수 있고, Solar API가 연결되면 같은 화면에서 질문 다듬기까지 사용할 수 있는 1차 출시 후보가 된다.
